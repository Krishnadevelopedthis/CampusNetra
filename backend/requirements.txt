# Pinned to minimums rather than exact versions: the project targets Python 3.14,
# where several older wheels are unavailable.

# --- Web framework ---
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
websockets>=14.0

# --- Database ---
sqlalchemy>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# --- Validation / settings ---
pydantic>=2.10.4
pydantic-settings>=2.7.0
email-validator>=2.2.0

# --- Auth & security ---
# bcrypt is used directly; passlib 1.7.x is incompatible with bcrypt >= 4.
bcrypt>=4.2.1
pyjwt>=2.10.1

# --- AI ---
anthropic>=0.42.0
openai>=1.0.0

# --- Media / matching ---
pillow>=11.0.0
numpy>=2.2.1

# --- Identity verification (name-change ID upload) ---
# Requires the Tesseract *binary* too — install it separately:
#   Debian/Ubuntu:  apt-get install tesseract-ocr
#   macOS:          brew install tesseract
#   Docker:         add the apt-get line to the image build
# Absence of either the package or the binary is handled gracefully — see
# app/services/id_verification.py — by queuing name changes for manual
# admin review instead of failing the request.
pytesseract>=0.3.13

# --- Object storage (STORAGE_BACKEND=s3) ---
# Optional: only needed if STORAGE_BACKEND is set to "s3" in config. Local-
# disk storage (the default) does not need this at all.
boto3>=1.35.0

# --- Utility ---
httpx>=0.28.1
python-dotenv>=1.0.1
tenacity>=9.0.0
greenlet>=3.2.0

# TLS certificate bundle — python.org builds ship no system CA store on macOS
certifi>=2024.0.0

# --- Testing (not required to run the application itself) ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
